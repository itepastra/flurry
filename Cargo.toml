[package]
name = "flurry"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.6.0"
tokio = { version = "1.38", features = ["full"] }
tokio-test = "*"

[profile.dev]
opt-level = 1

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
